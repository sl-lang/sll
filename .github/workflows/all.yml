name: All
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
permissions: read-all
env:
  BUILD_COMMAND: python src/_build/main.py --verbose
jobs:
  codeql:
    name: Analyze code
    needs:
      - diff
      - darwin
      - darwin-release
      - linux
      - linux-release
      - web
      - web-server
      - windows
      - windows-release
    if: always()&&!contains(needs.*.result,'failure')&&(fromJSON(needs.diff.outputs.data).linux||fromJSON(needs.diff.outputs.data).windows)&&github.event_name!='workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Initialize codeql@1
        uses: github/codeql-action/init@v1
        with:
          languages: cpp, javascript
      - name: Build
        run: ${{env.BUILD_COMMAND}}
      - name: Analyze
        uses: github/codeql-action/analyze@v1
  darwin:
    name: Build darwin
    needs: diff
    if: fromJSON(needs.diff.outputs.data).linux
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Build
        run: ${{env.BUILD_COMMAND}}
  darwin-release:
    name: Build darwin release
    needs: diff
    if: fromJSON(needs.diff.outputs.data).linux
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Build
        run: ${{env.BUILD_COMMAND}} --release --bundle --upload
      - name: Store assets
        uses: actions/upload-artifact@v2
        with:
          name: release-darwin
          path: darwin.zip
          retention-days: 1
  darwin-test:
    name: Test darwin release
    needs:
      - darwin
      - darwin-release
    if: github.event_name!='workflow_dispatch'
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Download release build
        uses: actions/download-artifact@v2
        with:
          name: release-darwin
          path: build
      - name: Unpack release
        run: unzip -o build/darwin.zip -d build
      - name: Test
        run: build/sll -I build/lib/stdlib.slb tests/_runner.sll
  diff:
    name: Calculate commit
    runs-on: ubuntu-latest
    outputs:
      data: ${{steps.calc_diff.outputs.table}}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Set-up requests
        run: pip install requests
      - name: Calculate
        id: calc_diff
        run: python src/_build/diff.py linux=src/sll/include,src/cli,src/sll,src/sll/platform/linux,tests windows=src/sll/include,src/cli,src/sll,src/sll/platform/windows,tests web-client=src/sll/include,src/web/client web-server=src/web/server ${{secrets.GITHUB_TOKEN}}
  linux:
    name: Build linux
    needs: diff
    if: fromJSON(needs.diff.outputs.data).linux
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Build
        run: ${{env.BUILD_COMMAND}}
  linux-release:
    name: Build linux release
    needs: diff
    if: fromJSON(needs.diff.outputs.data).linux
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Build
        run: ${{env.BUILD_COMMAND}} --release --bundle --upload
      - name: Store assets
        uses: actions/upload-artifact@v2
        with:
          name: release-linux
          path: linux.zip
          retention-days: 1
  linux-test:
    name: Test linux release
    needs:
      - linux
      - linux-release
    if: github.event_name!='workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Download release build
        uses: actions/download-artifact@v2
        with:
          name: release-linux
          path: build
      - name: Unpack release
        run: unzip -o build/linux.zip -d build
      - name: Test
        run: build/sll -I build/lib/stdlib.slb tests/_runner.sll
  release:
    name: Create release
    needs:
      - darwin-release
      - linux-release
      - windows-release
    if: github.event_name=='workflow_dispatch'
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Set-up requests
        run: pip install requests
      - name: Load assets
        uses: actions/download-artifact@v2
        with:
          path: assets
      - name: Create release
        run: python src/_build/release.py --verbose ${{secrets.GITHUB_TOKEN}}
  web:
    name: Build website
    needs:
      - diff
      - linux
      - linux-release
      - windows
      - windows-release
    if: always()&&!contains(needs.*.result,'failure')&&fromJSON(needs.diff.outputs.data).web-client&&github.event_name=='push'
    runs-on: ubuntu-latest
    env:
      ANALYTICS: ${{secrets.ANALYTICS_CODE}}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Build
        run: python src/_build/main.py --verbose --web
      - name: Store generated website
        uses: actions/upload-artifact@v2
        with:
          name: web-bundle
          path: web-bundle.dt
          retention-days: 1
  web-server:
    name: Upload website server
    needs: diff
    if: fromJSON(needs.diff.outputs.data).web-server&&github.event_name=='push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Set-up requests
        run: pip install requests
      - name: Upload
        run: python src/_build/website.py --verbose --server ${{secrets.CLOUDFLARE_ACCOUNT_ID}} ${{secrets.CLOUDFLARE_WORKER_NAME}} ${{secrets.CLOUDFLARE_TOKEN}}
  web-upload:
    name: Upload website
    needs: web
    if: github.event_name=='push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Set-up requests
        run: pip install requests
      - name: Load generated website
        uses: actions/download-artifact@v2
        with:
          name: web-bundle
      - name: Upload
        run: python src/_build/website.py --verbose ${{secrets.CLOUDFLARE_ACCOUNT_ID}} ${{secrets.CLOUDFLARE_NAMESPACE_ID}} ${{secrets.CLOUDFLARE_TOKEN}}
  windows:
    name: Build windows
    needs: diff
    if: fromJSON(needs.diff.outputs.data).windows
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Build
        run: ${{env.BUILD_COMMAND}}
  windows-release:
    name: Build windows release
    needs: diff
    if: fromJSON(needs.diff.outputs.data).windows
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup python@3.9
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
      - name: Build
        run: ${{env.BUILD_COMMAND}} --release --bundle --upload
      - name: Store assets
        uses: actions/upload-artifact@v2
        with:
          name: release-win
          path: windows.zip
          retention-days: 1
  windows-test:
    name: Test windows release
    needs:
      - windows
      - windows-release
    if: github.event_name!='workflow_dispatch'
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Download release build
        uses: actions/download-artifact@v2
        with:
          name: release-win
          path: build
      - name: Unpack release
        run: |
          Expand-Archive -Force build\windows.zip build\
          exit 0
        shell: powershell
      - name: Test
        run: build/sll.exe -I build/lib/stdlib.slb tests/_runner.sll
