(# int_type func_type)
(--- "types.sll")

(= pid (<- (... "sll:process_get_pid")))
(= shell (... "sll:process_execute_shell"))

(= process (. (&:
	(~ int_type) pid
	(~ func_type) shell
) pid shell))
(## process)
